<article class="card">
    <div class="card-body">
        <p>{{item.origin}}</p>
        <p>{{item.shortURL}}</p>
        <a href="/eliminar/{{item._id}}" class="btn btn-danger">Eliminar</a>
        <a href="/editar/{{item._id}}" class="btn btn-warning">Editar</a>
        <button data-copiar="{{item.shortURL}}" href="#" class="btn btn-info">copiar</button>
    </div>
</article>